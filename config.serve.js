const path = require('path')
module.exports = {
    paths: {
        vm_igb: '/flowpp/config/flowpp.ini',
        flowpp: '/flowpp/build/',
        properties: '/flowpp/config/',
        web: path.join(__dirname, '../')
    }
}
