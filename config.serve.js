const path = require('path')
module.exports = {
    paths: {
        vm_igb: '/flowpp/config/vm_igb.ini',
        flowpp: '/flowpp/',
        properties: '/flowpp/',
        web: path.join(__dirname, '../')
    }
}
